# Git Workflow

Após QUALQUER alteração, sempre executar:
- git add .
- git commit -m "descrição clara do que foi feito"
- git push origin main

Nunca encerrar tarefa sem push. O Vercel faz deploy automático após push no main.
